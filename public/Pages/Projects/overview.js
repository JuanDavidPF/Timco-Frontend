const urlParams = new URLSearchParams(window.location.search);
const projectKey = urlParams.get('projectId');


const ProjectLogo = document.querySelector();


const RenderProcectData = (projectKey) => {
    if (!projectKey) return;


}